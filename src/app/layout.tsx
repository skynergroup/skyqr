import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "QR Code Generator - Create QR Codes for WiFi, URLs, and More",
  description: "Generate QR codes for WiFi networks, websites, contact information, SMS, email, and locations. Easy-to-use QR code generator with instant download.",
  keywords: ["QR code", "generator", "WiFi", "URL", "contact", "SMS", "email", "location"],
  authors: [{ name: "Yash Sookdeo" }],
};

export const viewport = {
  width: "device-width",
  initialScale: 1,
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
